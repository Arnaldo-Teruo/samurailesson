-- SQLite
-- CREATE TABLE list(
-- 	id INTEGER PRIMARY KEY AUTOINCREMENT,
--   date TIMESTAMP DEFAULT (datetime('now','localtime')),
--   title TEXT,
--   col COL,
-- 	sort INTEGER
-- );

-- drop table fruit;

-- INSERT INTO list (title, col, sort) VALUES ("にんじん", "col3", 1);

-- update list set col = 'col1' where id = 1;

-- delete from list where id > 3;